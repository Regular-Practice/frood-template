{%- comment -%}
  Frood Recipes index — section-lockup header above a grid of recipe cards.
  Loops through every entry of the `recipes` metaobject definition.

  Note: the metaobject type handle is referenced as `shop.metaobjects.recipes`.
  If the type handle in admin is `recipe` (singular) instead, swap the dot-key.
  See CLAUDE.md → "Snippet Ownership" — `recipe-card` is owned by this section.

  Columns: 1 below 900px, 2 from 900px, 3 from 1800px. The 3-col jump sits well
  beyond the standard 1500px "large" breakpoint on purpose — these are wide
  horizontal (image + text) cards, so only very wide screens get three across;
  most desktops/laptops stay 2-up so each card keeps its width.
{%- endcomment -%}

<script type="module" src="{{ 'media-chrome.js' | asset_url }}"></script>
<script type="module" src="{{ 'recipe-modal.js' | asset_url }}"></script>

<section class="recipes-index">
  <header class="recipes-index-header section-lockup">
    {%- if section.settings.eyebrow != blank -%}
      <h1 class="text-h1">{{ section.settings.eyebrow }}</h1>
    {%- endif -%}

    {%- if section.settings.heading != blank -%}
      <h3 class="text-h3">{{ section.settings.heading | newline_to_br }}</h3>
    {%- endif -%}
  </header>

  {%- assign recipes = shop.metaobjects.recipes.values -%}

  {%- if recipes.size > 0 -%}
    <div class="recipes-index-grid">
      {%- for recipe in recipes -%}
        {% render 'recipe-card', recipe: recipe %}
      {%- endfor -%}
    </div>
  {%- else -%}
    <p class="recipes-index-empty text-body">
      {{ 'recipes.index.empty' | t }}
    </p>
  {%- endif -%}
</section>

{% stylesheet %}
  /* Mobile uses spacing-l on top (xl is too much air on a small screen);
     tablet+ steps up to xl. Inline + bottom paddings scale separately. */
  .recipes-index {
    padding-top: var(--spacing-l);
    padding-inline: var(--spacing-xs);
    padding-bottom: var(--spacing-xs);
    color: var(--color-text);
    background-color: var(--color-bg);
  }

  @media (min-width: 600px) {
    .recipes-index {
      padding-top: var(--spacing-xl);
    }
  }

  @media (min-width: 900px) {
    .recipes-index {
      padding-inline: var(--spacing-m);
      padding-bottom: var(--spacing-m);
    }
  }

  .recipes-index-header {
    margin-bottom: var(--spacing-l);
  }

  .recipes-index-header h3 {
    max-width: 36ch;
  }

  /* Mobile: 1 column */
  .recipes-index-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-xs);
  }

  /* Mobile + tablet (<900px): 1 column — cards take the full row width.
     Desktop (≥900px): 2 columns. Extra-wide (≥1800px): 3 columns. The 3-col
     jump is deliberately past the 1500px "large" breakpoint: these horizontal
     cards need width, so most laptops/desktops stay 2-up. */
  @media (min-width: 900px) {
    .recipes-index-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1800px) {
    .recipes-index-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .recipes-index-empty {
    padding: var(--spacing-l) 0;
    color: var(--color-text-accent);
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Recipes index",
  "tag": "section",
  "class": "recipes-index-section",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow (h1 — small uppercase label)",
      "default": "Recipes"
    },
    {
      "type": "textarea",
      "id": "heading",
      "label": "Heading (h3)",
      "info": "Press Enter to add a line break.",
      "default": "What we cook with our packs."
    }
  ],
  "presets": [
    {
      "name": "Recipes index"
    }
  ]
}
{% endschema %}
